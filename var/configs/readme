
ca.openvpn.crt              # openvpn root cert
ca.ipsec.crt                # ipsec   root cert
ta.tls-crypt.key            # global ta key for tls crypt option
ta.tls-auth.<GROUPNAME>.key # per servergroup ta key used with legacy tls-auth option
cl.<GROUPNAME>.crt          # client cert per group
cl.<GROUPNAME>.key          # client key per group
common.config               # openvpn config common for all servers and variants
servers.json                # server dataset for app
version                     # current version number
